public class IDK {
	public static void main (String[] args) {
		
		
		
	for(int i = 0; i < HobbyList.size() + 1; i++)
}
	
	if (userinput.equals("exit"))
		break;
	)
)

for(int i = 0; i < Hobbylist.size() +1; i++)
	for(int i = 0; true; i++)
		while (true)
			
			
			most common way is:
				while(i userinput.equals("exit")) (
					
						)
	}
}			
	// for question 11, asked to prompt user to enter either the word exit, or one hobby of his/hers.
	// then display all hobbies until the user enter's "exit". Then display all the hobbies that the
				// user just entered(one hobby per line.) 
	// forgot to establish arraylist, and need to add hobby to arraylist. 
				
	// System.out.println(hobby) needs to be in a loop. 